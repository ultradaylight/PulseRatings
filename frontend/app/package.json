{
  "name": "pulse-ratings-dapp",
  "version": "1.0.0",
  "description": "A dApp to interact with PulseRatings contract on PulseChain Testnet v4",
  "main": "app.js",
  "scripts": {
    "start": "npx serve .",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./css/output.css --minify",
    "watch:css": "npx tailwindcss -i ./src/input.css -o ./css/output.css --watch"
  },
  "dependencies": {
    "ethers": "^6.13.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "serve": "^14.2.3",
    "tailwindcss": "^3.4.17"
  }
}
